{
    "version": "0.2.0",
    "configurations": [
        {        
            "type" : "PowerShell",
            "name": "Build and Run X16 Emulator (Debugger)",
            "request" : "launch",
            "script" : "./build.ps1",
            "cwd": "${workspaceFolder}"
        }
    ]
}